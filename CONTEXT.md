# observex 上下文

本文件定义 `observex` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**观测实现**：把 `instrumentationx::Instrumentation` 的注入事件落到具体观测设施的一方；
它只实现契约，不定义契约。
_Avoid_: SDK（SDK 通常还包含配置面、采样与后端供应链）

**op 名治理**：对 `op` 做 trim、移除控制字符、空值回落 `_` 并限长的就地规整。
_Avoid_: PII 检测、allowlist（两者都超出本 crate 边界，本 crate 不做敏感性判断）

**有界进程内 sink**：只保存在当前进程内、按信号设置固定事件容量上限的遥测缓冲。
_Avoid_: exporter（OpenTelemetry exporter 含远端发送语义，本类型不发送）

**非 OTEL 边界**：明确声明**不是** OpenTelemetry API/SDK、语义约定或 OTLP exporter。
_Avoid_: 兼容 OTEL（会误导调用方期待 OTLP 信封与生命周期语义）

## 记录路径与语义

**op 深度**：以 `.` 分段计数的 op 层级数；空值或归一后的 `_` 视为 1 层。
_Avoid_: 路径长度（长度是字节数，不是层数）

**op 叶子**：`op` 的最后一段，用于按动作维度聚合。
_Avoid_: 操作类型（叶子是位置概念，不保证语义类型）

**限长标记 `~`**：截断发生时追加在结果末尾的哨兵，提示值已被裁剪而不完整。
_Avoid_: 省略号（`~` 是机器可判定的固定标记，便于下游识别）

**前缀包装**：给 `op` 加统一前缀后再委托内层实现，用于隔离命名空间；前缀本身也参与清理与限长。
_Avoid_: 装饰器（装饰器不表达「前缀参与 sanitize」这一语义）

**计数实现**：进程内原子计数，仅用于单元测试断言；它不是生产 metrics。
_Avoid_: metrics 后端（它不聚合、不导出、不持久化）

## sink 语义与失败

**每类信号容量**：span 与 metric 各自独立的槽位数，默认 1024；容量限制的是事件数量，
不限制单个事件字段的字节数。
_Avoid_: 总容量（两类信号容量互相独立，不能合并理解）

**整批拒绝**：单次同类批次容量不足时全批不写入、原缓冲不变，整批长度计入 dropped。
_Avoid_: 部分写入（部分写入会破坏 all-or-nothing 语义）

**一致性快照**：在同一 mutex 临界区内一次性读取 buffered / flushed / dropped / shutdown
所得的一致读数。
_Avoid_: 实时监控（快照是某一时刻的一致读数，不是持续采样）

**计数饱和**：累计计数溢出时字段饱和为最大值，并由标记指出该值只能解释为下界。
_Avoid_: 精度丢失（丢失意味着数值不可信，饱和是把数值明确标记为下界）

**诊断内化**：wrapper 把 exporter 返回的 `Err` 与可展开（unwind）的 panic 转为诊断计数，
不改变记录调用的返回、也不重试。
_Avoid_: 重试（wrapper 明确不重试，失败事件只记为 unconfirmed）

**flush / shutdown 边界**：flush 只清空进程内缓冲并累计计数，不代表写盘或远端确认；
shutdown 在同一临界区先 flush 再幂等关闭。
_Avoid_: 持久化确认（两者都不落盘、不远程发送）
